// @ts-check

import mdx from '@astrojs/mdx';
import sitemap from '@astrojs/sitemap';
import { defineConfig, fontProviders } from 'astro/config';

// https://astro.build/config
// TODO: troque SEU_USUARIO e NOME_DO_REPO abaixo pelos valores reais assim que criar o repositório.
// Se o repo se chamar "SEU_USUARIO.github.io", o site fica na raiz e `base` deve ser removido.
export default defineConfig({
	site: 'https://SEU_USUARIO.github.io',
	base: '/NOME_DO_REPO',
	integrations: [mdx(), sitemap()],
	fonts: [
		{
			provider: fontProviders.local(),
			name: 'Atkinson',
			cssVariable: '--font-atkinson',
			fallbacks: ['sans-serif'],
			options: {
				variants: [
					{
						src: ['./src/assets/fonts/atkinson-regular.woff'],
						weight: 400,
						style: 'normal',
						display: 'swap',
					},
					{
						src: ['./src/assets/fonts/atkinson-bold.woff'],
						weight: 700,
						style: 'normal',
						display: 'swap',
					},
				],
			},
		},
	],
});
